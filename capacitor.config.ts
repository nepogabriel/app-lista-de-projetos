import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lista_de_contatos',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
